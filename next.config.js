/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ['drowsier-amelia-unimpoverished.ngrok-free.dev'],
  // Configurações adicionais se necessário
};

module.exports = nextConfig;
